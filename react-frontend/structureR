react-frontend/
├── src/
│   ├── api/               ← Axios API calls (e.g., users.js, auth.js)
│   ├── components/        ← Reusable components (e.g., Modal, Table, Navbar)
│   ├── modules/           ← Feature modules (e.g., users, bookings)
│   ├── pages/             ← Page-level components (UsersPage, DashboardPage)
│   ├── App.js             ← Routing and layout
│   ├── main.jsx           ← App bootstrap


// Directory structure
// src/
// ├── components/
// │   ├── Auth/
// │   │   ├── LoginForm.jsx
// │   │   ├── RegisterForm.jsx
// │   │   └── AuthPage.jsx
// ├── pages/
// │   └── AuthPage.jsx
// ├── routes/
// │   └── PrivateRoute.jsx
// ├── api/
// │   └── auth.js
// ├── App.jsx
// └── main.jsx


/src
  ├── components/
  │   ├── Sidebar.js
  │   ├── Header.js
  │   ├── LoadingScreen.js
  │   ├── MainContent.js
  │   ├── ReservationAlertButton.js
  │   └── ProtectedRoute.js
  ├── views/
  │   ├── Dashboard.js
  │   ├── Users.js
  │   ├── Rooms.js
  │   ├── Bookings.js
  │   ├── Payments.js
  │   ├── Events.js
  │   └── ReservationAlerts.js
  ├── services/
  │   └── api.js
  ├── App.js
  └── index.js


info install
npm install react-icons


now lets work on something again, in the dashboard display content, i have the room status display session and the bar chart session. i want to replace that bar chart  session to payment status session, so as the guest payment i posted it will display a green as payment, the display will look like the room status box like display but in smaller form, we may have color display as this, since we have payment status as 
fully paid : green
part payment: dark grey
pending: light gray 
complimentary:


if you go thru our previous chat we have dealing on restaurant endpoint, now we have to move to list payment, we will the same folder src/components/restaurant ListRestaurantPayment.jsx and css, you will use the import function, import axiosWithAuth from "../../utils/axiosWithAuth"; 
will have the action button. edit, void, print, i will send you the backend list payment and update 